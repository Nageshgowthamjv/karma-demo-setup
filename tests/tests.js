// tests.js
describe('Array', function(){
  describe('#indexOf()', function(){
    it('should return -1 when the value is not present', function(){
      chai.assert.equal(-1, [1,2,3].indexOf(5));
      chai.assert.equal(-1, [1,2,3].indexOf(6));
    });
  });
});

describe('Array', function(){
  describe('#indexOf1()', function(){
    it('should return -12 when the value is not present', function(){
      chai.assert.equal(-1, [1,2,3].indexOf(5));
      chai.assert.equal(-1, [1,2,3].indexOf(6));
    });
  });
});

describe('Array', function(){
  describe('#indexOf2()', function(){
    it('should return -13 when the value is not present', function(){
      chai.assert.equal(-1, [1,2,3].indexOf(5));
      chai.assert.equal(-1, [1,2,3].indexOf(6));
    });
  });
});